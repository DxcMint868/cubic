# MEMORY.md

## Purpose

This is the project's hot-memory layer: the actively maintained working fact store for agents. It records implementation state, decisions, current tasks, constraints, discoveries, unresolved questions, and project nuances that should survive across agent sessions.

## Agent Instructions

- **MUST read MEMORY.md at the start of every substantive task.**
- **MUST update MEMORY.md when implementation state, architecture, decisions, tasks, constraints, or important technical facts change.**
- Prefer concise, factual entries over prose.
- Record decisions with enough context to prevent later agents from accidentally reversing them.
- Mark stale or superseded facts clearly rather than silently deleting useful history.
- Do not use MEMORY.md as a dump for transient chatter. Keep it useful as working memory.
- When uncertain whether a fact is current, verify it before treating it as authoritative.

## Current State

Update this section as implementation progresses.

### Active Work

Status as of 2026-09-10: the marketing/landing site (header, hero, manifesto, team section, footer) is built and iterated to user satisfaction. All items below are platform work that has NOT started — no gateway, no integrations, no real telemetry yet (hero/landing stats are mock per PROJECT.md §10).

- [x] Bootstrap planning complete — 11 plan files in `.agents/plans/` (see Recent Changes). Execution not started.
- [x] Plans hardened to code-level specs (2026-09-10): every plan now carries **EXACT** verbatim contracts (full 13-table drizzle schema, zod event payload schemas for all 17 event types, policy rule JSON documents + rule semantics + selection algorithm + evaluation order, normalizer mapping table, capability issue/consume algorithms with ordered reject reasons, executor + payment + approval + secret-protector interfaces, projection mapping table, SSE/stats shapes, API envelopes with literal response JSON) plus given/when/then test tables with literal expected values (decision / matched_rule_id / reason code) and curl verification commands. plan-00 gained shared domain types (`domain.ts`), ReasonCode enum, API error-code enum, and the binding "EXACT blocks are copied, never redesigned; failure → stop and report" rule.
- [x] Orchestration set up: `.agents/dispatch/` contains `RUNBOOK.md` + verbatim dispatch prompts plan-01…plan-10 (fences, git discipline, test-tenant rules, done criteria). Waves: W1 01 solo → W2 02 solo → W3 03∥08 → W4 04∥07 → W5 05∥06 → W6 09 solo → W7 10 solo. Worktree per plan REMOVED (simplified 2026-09-10): solo waves and parallel waves alike get a sibling worktree per active plan inside the OpenChamber container (`~/.local/share/opencode/worktree/b95ec527e96af9bfaa762da13fa0336717652522/plan-XX`, so they show in its sidebar), branch `plan-XX`, created per wave off latest main with deps installed + `.env.local` copied in, deleted after merge — agents never run in the home repo; home `main` stays pristine (merges only). Merge order within a wave = lower plan number first. Agents never edit MEMORY.md (merger folds their `## Spike findings` into it); plan-10 is the sole final-update exception.
- **Self-review mandate (2026-09-10)**: every implementation agent spawns its own council mid-work (Explorer-type: consumption tracing vs plan-00 contracts; General-type, adversarial: prove the step wrong) and runs the `contract-reviewer` gate pre-report, fixing to MERGE; inline fallback with written notes if its harness lacks subagents. The merger independently re-runs the gate — the agent's self-gate never replaces it. Mandate lives in all dispatch prompts (plan-02…plan-10) + RUNBOOK loop.
- [x] **W1 plan-01 MERGED 2026-09-10** (merge 46b7d2f): foundation live — 13-table schema migrated, event bus (17 types), seed, health, 23/23 tests green on live Neon DB. Agent was honest about the dead-DB block instead of faking green; orchestrator fixed 2 latent fresh-DB test bugs (null-safe demoCounts, FK-safe afterAll cleanup, 30s latency timeout). Gate returned MERGE. Deviations logged for later waves: (1) payload optionals implemented as required-nullable (emit the nulls); (2) seed creates demo tenant if missing; (3) dotenv lives in devDeps; (4) vitest fileParallelism:false for the shared DB. **schema.ts FROZEN from here on.** Next: W2 plan-02 gateway+policy.
- [x] **W2 plan-02 MERGED 2026-09-10** (merge 36736c9): gateway live — ingest, normalize (incl. unknown-tool fallback), pure policy engine (3 rule docs, selection, first-match-wins), dev approvals, audit events + trace routes. 37/37 tests green live; allow/deny/escalate + trace verified via curls. Gate returned MERGE. Notes: (1) agent never committed (user committed f118c66) and showed no council/gate evidence — from W3 on, "done" pings must attach the gate verdict + council summary or the wave goes back; (2) orchestrator wrongly reverted the vitest @ alias (missed @/ imports in untracked route files — `git grep` skips untracked), restored in dda8cb2 — lesson: verify with worktree-wide search, not git-grep; (3) `service_allowlist` null-guard in engine is stricter than the plan formula — informational, unobservable this wave. Next: W3 plan-03 ∥ plan-08.
- [x] **W3 MERGED 2026-09-10** (merges 0ccb9a3 plan-03, 79f06e1 plan-08 — zero conflicts, disjoint files). Capabilities live (issue/verify/consume/revoke, 5m TTL, policy_hash over document, ordered rejects); network projection live (17-type mapping, pseudonym salt, SSE, stats, real-pipeline swarm). Checks: 50/50 + 46/46 green live; both gates MERGE. Notes: (1) `not_found` consume returns WITHOUT emitting `capability.rejected` (no attributable tenant chain — plan-03 step 2 "each rejection emits" exempted); (2) `loadPolicyDocument` export is a plan-06 forward-hook — plan-06 must consume or inline it; (3) canary test puts CANARY token in `resource` not redacted args — equivalent proof, accepted; (4) user ran agents with no council/gate evidence attached again — mandate restated, W4 pings without verdicts go back. Next: W4 plan-04 ∥ plan-07.
- [x] **W4 MERGED 2026-09-10** (merges cfeebfd plan-04, 6c256a9 plan-07 — one trivial conflict, two timeout-comment lines, took plan-07 side). Executors + MCP facade live (73/73); Agent0 client + graph provider live (66/66); both gates MERGE. Notes: (1) full-suite flakes under shared-Neon contention — merger now verifies serially, one worktree at a time; parallel verification caused the only red runs this wave; (2) plan-07 `loadPolicyDocument` forward-hook consumed as designed; (3) wallet decision RECORDED: user-supplied Hedera testnet dev wallet is the payment authority (EVM `0xd682…73EE`, account `0.0.10481126`, 10 HBAR confirmed on mirror node, hollow account with null key — first payer-signed payment auto-completes it; orchestrator-minted keypair superseded, never funded, never used). Key lives ONLY in `app/.env.local` (gitignored) — never in chat/logs/commits; `HEDERA_OPERATOR_ID` + `HEDERA_NETWORK=testnet` alongside. Next: W5 plan-05 ∥ plan-06.
- [x] **W5 MERGED 2026-09-10** (merges a670146 plan-05, 32f9f9c plan-06). Real HBAR settled on testnet via Blocky402 (live test green, mirror-verified); gates: contract MERGE + PO SHIP-WITH-GAPS on both. Merger resolved the 05/06 orchestrator collision (both refactored it): kept plan-06's shared-phase structure, integrated plan-05's purchase branch once — caught and fixed 3 live bugs the collision created (unconditional payment_required nulling, missing service.discovered emit, purchase-vs-generic double execution via new origin threading). Full suite green on merged code: 12 files, 96 pass, 2 skips (keyring device skips). PO asks closed or transferred: ledger.md live-run disclaimer added; settlement-receipt strengthening + 25¢ video path → plan-10; Beat-4/5 rendering → plan-09 phase B. Operator wallet down to ~0.03 HBAR — top up at portal.hedera.com before demo/video.
- [x] **Upstream UI merged 2026-09-10** (merge 7b1ae38, zero conflicts, typecheck green): landing overhaul (SiteHeader/Footer, HeroCopy, Manifesto, MarketSection, PartnerMarquee, Closer, FAQ, WalletProvider) + `/network` + `/console/**` shell pages. CRITICAL: upstream pages run on STATIC MOCK data (`app/src/data/agents.ts`, `console.ts` — no fetch, no /api calls). Reconcile direction: keep their shell/components/design, REWIRE data to real gateway APIs — this becomes plan-09's job (redirect relayed to the running agent + merged into its branch).
- **plan-09 worktree vanished mid-flight (2026-09-10, cause unknown — NOT merger action)**: dir + branch gone, zero plan-09 commits anywhere (reflog/fsck checked; only old stash WIPs dangling). Phase-A work unrecoverable. Silver lining: W5 is merged, so the A/B split is obsolete — recreated `plan-09` worktree fresh off post-W5 main for a FULL plan-09 run with `.agents/dispatch/plan-09.md` (ignore plan-09a.md). If the 09a agent reports later, treat its report as advisory only.
- [x] **W6 plan-09 MERGED 2026-09-10** (merge 9e8ae51): full console + live network viz rewired from upstream mock shell to real APIs — TraceView (DENY reasons, payment leg with settlement ref, approvals with provider badge), actionable approval queue, SSE network with legend + swarm honesty tag. Checks: 13 files, 104 pass, 2 skips; gates: contract MERGE + PO SHIP-WITH-GAPS. PO asks transferred to plan-10: (1) delete dead mock files (`data/agents.ts`, `data/console.ts`, `AgentGraph.tsx` — unreachable but present); (2) unify AUTHORIZED to ALLOWED; (3) MacWindow-wrap Agent/Decision detail; (4) live end-to-end proof on camera (incl. confirming x402_ref). Next: W7 plan-10 (final wave).
- [x] **Plan-11 AgentGate mesh MERGED 2026-09-12** (merge 04e1778): LangSmith reasoning links (passthrough + traceable assist, null-safe, never mocked), treasury branch (swap/payroll/stake-pair split on $100 notional via existing rules, dev-labeled), carried PO polish (dead mocks deleted, ALLOWED unified, MacWindow wraps, console URLs). Gates: contract MERGE + PO SHIP-WITH-GAPS. PO narration asks (budget-vs-notional line, $450k framing lock, LangSmith take decision) transfer to video prep. Full suite green (16 files). NO schema/engine/reason-code changes. Build complete — all waves W1-W7 + plan-11 merged.
- [x] **LangSmith live leg GREEN 2026-09-12** (first real pass ever — previously always skipped for no-key): THREE stacked root causes, none the key itself — (1) `cubic-demo` project didn't exist (created via API, 200); (2) `LANGSMITH_TRACING` never set and SDK defaults tracing OFF (`getCurrentRunTree()` returns id-less stub — missed by plan-11 spec, agent, both gates, merger); (3) fresh runs 404 on read-back ~4s (ingestion lag) — honesty gate now retries 3x2s inside the 8s budget. Fixes committed (f4ea953): test sets flag, `.env.example` documents it, demo checklist carries keyed-vs-unkeyed decision. Live-key green 15.4s, real run_id read-back verified.
- [x] **Plan-16 FILED 2026-09-12 (founder-ordered scope)**: UI demo flow micro-wave — chat + templates + Play + tasks filters + runbook rewrite + HCS fingerprint anchoring (async projection pattern, per allowlisted events, fingerprint derived in trace not stored, topic script post-top-up, warn-only failure) + templates for already-enforced branches (cross-task/unknown-tool DENYs, replay/expired rejects, drain-reject, deploy escalate — ZERO engine changes; new rule types stay plan-15 post-video). Founder clarifications banked: frozen engine means done-not-cut; DAG-graph history deferred post-video.
- [x] **Plan-14 graph-live WIRED 2026-09-12** (commit 9cdedd6, direct merger execution — no wave needed): real Agent0 Base endpoint defaulted (subgraph `43s9hQR…`, verified live; Hedera has no ERC-8004 contracts — read-on-Base/enforce-on-Hedera stated in code), negative on-chain scores accepted (zod was rejecting real -50 penalty feedback → silent neutral; clamp already handled them), selection engages on key-or-URL, lab-1 flipped to live `8453:74108` (rep ~0.10, escalates for real), fixture demoted to tested offline fallback. Full suite 19 files / 158 green on local PG. Caught live: 2 outdated tests rewritten to new contracts, 1 env-coupled test isolated, 1 mocked-path timeout bumped (6s observed, mechanism open). Fixture-era is over for reputation.
- [x] **Plan-12 MERGED 2026-09-12** (merge a146db0): protector seam + dev backend, event-sourced approver identity, capability promo, preflight asserts, HashScan link. 6-reviewer security council convened (PO + contract + tracer + security + integration + AI-engineer): PO MEETS-WITH-GAPS (DetailsSection overclaim fixed by the plan-12 agent itself pre-merge — fence-external file, accepted), contract MERGE, tracer mapped full flow (1 dangerous finding: error_code refactor hazard + camelCase regex gap), security SEALED-dev-labeled (runbook/boot-gate/redaction asks), integration GRAFTED (revoke silent, resolve divergence, completed→failed overwrite), AI SOUND-WITH-RESERVATIONS (seam right, 3 reservations). Core answer to founder's fear: NO path exists today for operator plaintext to reach LLM context/training/KV — proven hop-by-hop; gaps are absent guards, not live leaks. Next: plan-13 (council hardening micro-wave) paved and ready.
- [x] **W5b plan-06 GATE: MERGE (merge HELD pending plan-05)** — 4 commits, clean tree. Spike honest: wallet-cli 2.1.0 installed + ring subcommands pinned, hardware path BLOCKED (no device), docs state it. Resolve route + keyring (throws, never silent-fallback) + `loadPolicyDocument` hook consumed. Gate proved the 202-line orchestrator extraction behavior-preserving (37 isolated regression passes). Isolated suites green (ledger 5+2skips, network 9/9); full-run flakes are shared-Neon contention only. MERGE in wave order AFTER plan-05 lands.
- [x] **PO review wired late + plan-06 PO verdict (2026-09-10)**: root cause of user's catch — `product-owner.md` existed on disk but ZERO dispatch prompts referenced it, so no session ever called it (my miss, not the agents'). Fixed: PRODUCT-REVIEW sections in dispatch plan-05/09a/09/10 (what PO gates + how to pre-empt), merger PO-gate step in RUNBOOK loop. Plan-06 PO verdict: SHIP-WITH-GAPS (Beat-5 story runs on dev path; no creep/cuts; terminology clean; honesty benchmark). Binding asks recorded: (1) one-liner in `docs/ledger.md` — keyring success path never ran live — merger adds at plan-06 merge time; (2) plan-09 must render escalation reason + approval queue + provider badge; (3) plan-10 narration frames resolve as dev stand-in + uses PROJECT.md §17 numbering (Ledger = Beat 5, payment = Beat 4 — my earlier messages had it backwards).
- **Product-owner profile added (2026-09-10)**: `.opencode/agents/product-owner.md` (mode subagent, temp 0.7, edit/write ask) — PM/PO voice for council rounds and wave reviews (demo beats, scope fit, DESIGN.md, sponsor risk; SHIP/SHIP-WITH-GAPS/BLOCK-FOR-PRODUCT). Second profile after contract-reviewer; implementer profiles still rejected.
- **Worktree GitNexus hiccup (2026-09-10, user-found)**: plain `npx gitnexus analyze` inside a worktree silently indexes nothing — later GitNexus commands fail. Fix: every worktree session runs `npx gitnexus analyze $(git rev-parse --show-toplevel)` FIRST (explicit toplevel path). Baked into dispatch prompts plan-04…plan-10 (01–03, 08 predate it), RUNBOOK prep loop, and orchestrator pre-indexes each new worktree. AGENTS.md's auto-managed GitNexus block left untouched.
- [x] **W7 plan-10 DONE 2026-09-12 (merge pending)**: demo + adversarial flows live — `app/src/server/demo/agent.ts` (HTTP happy path 11 steps + in-process `--adversarial` 6 fixtures), `POST /api/demo/run` (server-side happy path, mirrors resolve route incl. 409 fidelity), `app/tests/e2e.demo.test.ts` (20-event chain as documented subsequence + all 6 reason codes), `docs/demo.md` (beat sheet §17 numbering, checklist, commands, recovery), `demo`/`demo:adversarial` scripts in `app/package.json` (fence exception: plan-required invocation names, 2 lines, declared). Checks: typecheck clean, lint exit 0 (2 pre-existing warnings), full suite 14 files green (112 pass pre-fix; e2e re-green 7 pass + 1 honest `ctx.skip()` after). Council ran (Explorer MERGE, Adversary BLOCK→fixed vacuous-pass live test + 10 nits, 2 rebutted as out-of-fence) + contract-reviewer gate MERGE. Plan-10 ACs ticked.
- **Video-take blocker RESOLVED 2026-09-12 (user: "fix it here now" — fence break authorized, changes on plan-10 branch for merger visibility)**: root cause was NOT facilitator drift — `payToAccount()` already supported a distinct merchant (`X402_PAY_TO_ACCOUNT`, plan-05 commit `2017295`), but the gitignored `app/.env.local` in this worktree never got the line, so every payment was operator→operator self-pay, which nets to zero and the facilitator correctly rejects with `amount_mismatch` (verified against the SDK's own verify logic + live probes at 1¢/25¢). Fix = one env line: `X402_PAY_TO_ACCOUNT=0.0.10482549` (merchant verified on mirror, ~5.95 HBAR, key discarded/receive-only). Proof: two live 25¢ settlements settled post-fix — merchant 595M → 1,266M tinybars (+670M = exactly 2×335M, mirror-verified), operator 9.73 → 3.03 HBAR. Second latent bug on the same path: `GithubExecutor` had no `deploy_production` case (seed wires deploy→github) — every approved deploy died with "unsupported action"; added mock-always case (no real deploy target in MVP; labeled mock, authorization chain is the real behavior). NOTE: no `.env.example` exists in repo — merger must ensure `X402_PAY_TO_ACCOUNT=0.0.10482549` lands in every deploy env (public account id, not secret).
- **To finish the proof (needs ~1 HBAR top-up)**: operator sits at ~3.03 HBAR vs ~3.35 required per 25¢ run — the e2e live test now honestly skips (guard works after the `?balance=true` fix). One funded run of `tests/e2e.demo.test.ts` completes the full 20-event chain green. Total spent proving this: 50¢ (2 settlements, both to the merchant).
- **Latent bug found 2026-09-12 (fixed in plan-10's e2e, STILL PRESENT in plan-05's live test)**: mirror `GET /api/v1/accounts/<id>?balance=true` now 400s ("Unknown query parameter") — balance guards using it read 0 and skip forever. Fixed in `app/tests/e2e.demo.test.ts` (plain endpoint + `res.ok` check); `app/tests/x402.live.test.ts` has the identical pattern — plan-05 owner should apply the same one-line fix.
- **Demo tenant found EMPTY once 2026-09-12 (no agents/tasks; cause unclear — possibly an interrupted seed)**: re-seeded via `demo:adversarial` (green); probe scripts that assumed fixture state now seed first. If it recurs, suspect concurrent seeds racing (seed wipes then inserts).
- **PO asks NOT done (out of plan-10 fence — left for merger/follow-up)**: plan-09 items (1) delete dead mock files (`data/agents.ts`, `data/console.ts`, `AgentGraph.tsx` — confirmed still present), (2) AUTHORIZED→ALLOWED unification, (3) MacWindow-wrap Agent/Decision detail. Plus tech-debt: shared `resolveApproved` helper (demo/run duplicates the resolve-route approved arm by design — extraction touches plan-06's file).
- [x] **W7 plan-10 MERGED 2026-09-10** (merge 4b3d84c): demo script (11 steps, preflight aborts), 6 adversarial fixtures, e2e ordered chain, `POST /api/demo/run`, `docs/demo.md`, MEMORY end-state entry. Agent found+fixed 2 live bugs (self-pay settlement rejection via missing merchant env; deploy_production executor gap). Gates: contract MERGE (+logging.ts exception accepted) + PO SHIP-WITH-GAPS. Merger follow-ups committed (1199ae9): mirror `?balance=true` fix in x402.live (was skip-forever), merchant line in demo checklist, mock-deploy disclosure, `app/.env.example` created. Live proof: x402.live 2/2 green on merged main (real 1¢ Blocky402 settlement). Full suite: 15 files, 114 pass, 3 skip. Remaining before camera: ~1 HBAR top-up, funded e2e green run. Next: plan-11 (LangSmith links + treasury demo branch + carried PO polish: dead mocks, ALLOWED unify, MacWindow wrap, console trace URL).
- [x] **Plan-13 MERGED 2026-09-12** (merge b25257e): error-enum sanitization (evil-twin canary), `capability.revoked` events + projection, ledger boot-gate + explicit migration error, trace-link origin labels, compromise runbook + exposure SQL. Verified on FRESH LOCAL Postgres (user-swapped DATABASE_URL): migrated clean, seeded (2/6/3/1), 155 pass / 2 skip / 1 pre-existing env fail (treasury live-key 401 — key invalid in env, fails identically reverted). Gates: contract MERGE + PO SHIP-WITH-GAPS. PO asks transferred (docs, non-blocking): CLIENT-tag definition + enum-debug runbook line still open (e2e-chain wording already matches code — verified). Local DB is now the verified baseline — Neon retired.
- [ ] Implement the authorization gateway and capability model.
- [ ] Integrate MCP as the tool boundary.
- [ ] Integrate ERC-8004 / The Graph context.
- [ ] Integrate Ledger Key Ring / Agent Stack path.
- [ ] Integrate a live Hedera x402 payment flow.
- [ ] Build tenant control-plane surfaces.
- [ ] Build the global agent-network visualization (the hero's NetworkCanvas is decorative ambience, not this surface).

### Decisions
- Monorepo managed with **pnpm workspaces** (root `pnpm-workspace.yaml`).
- `app/` is a Next.js 15 App Router project (React 19, TypeScript 5).
- `contracts/` is a Foundry/Solidity project (forge 1.5.1, solc 0.8.24).
- `pnpm dev` / `pnpm build` / `pnpm typecheck` run app commands from root.
- `pnpm forge:build` / `pnpm forge:test` run contract commands from root.
- **Plans convention**: every implementation plan lives in `.agents/plans/` as a markdown file with front matter `guide:` referencing **exactly one** `.agents/guides/*.md` file, plus tasks and acceptance criteria. Current set: `plan-00-architecture` (shared baseline: data/event model, API contracts, integration boundaries) + `plan-01-foundation` → `plan-10-demo-adversarial` (build-order slices), all referencing `.agents/guides/guide-01-bootstrap.md`.
- **Gateway placement**: gateway runtime lives in `app/src/server/` (plain server modules, Next.js route handlers as thin adapters). No third root package.
- **Storage**: online PostgreSQL via user-supplied `DATABASE_URL` (in `app/.env.local`, never committed); drizzle-orm + postgres.js + drizzle-kit migrations; zod for validation. Seed scripts must only touch demo-tenant rows (shared DB).
- **No new Solidity contracts in the MVP** (identity/reputation via Agent0/ERC-8004 subgraphs; Hedera requirement is the live x402 service + Blocky402 settlement). Foundry scaffold stays untouched.
- **Testing**: vitest for server units (introduced in plan-01); root gains `pnpm test`.

### Recent Changes
- **Council debate on opencode profiles (2026-09-10)**: PRO wanted 3 profiles, CON wanted zero, PRAGMATIST won — exactly ONE profile created: `.opencode/agents/contract-reviewer.md` (mode subagent, temp 0.2, edit+write denied, bash allowed for verification commands; model pinned to opencode-go/glm-5.3-flash per user override, accepted 2026-09-10 (fast/cheap gate; orchestrator merger review backstops semantic misses the smaller model might wave through)). Pre-merge gate every wave: EXACT conformance, AC evidence, green checks, fence/secrets hygiene, MERGE-or-BLOCK verdict, never edits. Implementer profiles rejected (dispatch prompts are more specific; would fork truth). Revisit only on observed fence breach, drift behind green tests, or a mechanical wave. NOTE: opencode loads agent config at startup — restart opencode after adding this file.
- **Round-table review applied (2026-09-10)**: spawned 3 parallel review subagents (red-team, weak-model simulation, contract audit) against all 11 plans. They converged on 3 blockers + ~15 majors, ALL FIXED pre-W1: (1) `ExecutorResult` union now expresses `payment_required` with no-execute/no-consume semantics; (2) ingest threads `origin` (default `"agent"`); (3) escalate path emits `ledger.approval.requested`; (4) `decisions.matched_rule_id` column added (single permitted pre-freeze schema change — schema now frozen); (5) `load-env.ts` (dotenv) imported first in config/drizzle/vitest/tsx entrypoints — `.env.local` loads everywhere; (6) `issueCapability` takes the policy document + decisionId and does the approval-gate DB lookup internally; (7) unknown tools fall through normalize to a policy `tool-allowlist` deny (no 4xx); (8) 402 body nested under `error` per envelope; (9) projection resolution order + swarm self-seed + reachable clusters; (10) `api/demo/run` owned solely by plan-10; `api/network/stats` added to plan-00 §G; swarm/agents fallback paths made primary. Deliberately NOT fixed (spike-gated by design): x402 package names, wallet-cli commands, Agent0 query fields.
- **Bootstrap planning complete (2026-09-10)**: created 11 implementation plans in `.agents/plans/` — `plan-00-architecture.md` (shared baseline: module layout under `app/src/server/`, 13-table Postgres schema, canonical 17-type event model, API route table, MCP/Ledger/Hedera/Graph boundaries, "One Thing" demo flow) plus `plan-01-foundation` → `plan-10-demo-adversarial` following the guide's build order. All reference `.agents/guides/guide-01-bootstrap.md`. Notable environment facts recorded: `wallet-cli`/`ledger-cli` NOT installed (plan-06 starts with an install/verify spike); x402/Hedera package names must be verified in a plan-05 spike; GitNexus index name is stale.
- Git init, monorepo structure, pnpm workspaces, Next.js App Router scaffold, Foundry project scaffold — all verified building clean.
- Landing page (`app/src/app/page.tsx`) replaced with a pure black full-screen canvas; user is iterating on a logo concept from there. First `pnpm install` run at app level (node_modules was missing).
- Logo concept (network squares forming a "C") shelved by user — instead built split landing page: left = product name + one-liner, right = live square-network canvas (`app/src/components/NetworkCanvas.tsx`, 2D canvas: jittered grid nodes, connective lines, traveling light pulses). Added `app/src/app/globals.css` (reset, black bg, blink keyframe). Layout is inline-styled for now, no design system yet.
- Project name confirmed: **Cubic**. Logo direction chosen: wireframe isometric cube with the front face open ("the gate"). Isolated work-in-progress at `/playground` (`app/src/app/playground/page.tsx`) — SVG + tiny 3D projection, pointer-tilt to inspect, edge opacities tunable (front face currently alpha 0.14, back/connectors 0.9).
- User rejected v1 (plain wireframe cube = "a diagram, not a mark"; wants 3D depth but no interactivity). v2 now at `/playground`: "the nest" — outer wireframe cube with the front-top edge removed (the gap), solid shaded inner cube (per-face lighting) inside nudged toward the gap. Static.
- User rejected v2 as well — logo exploration shelved for now, `/playground` deleted. Open question: brand mark still unresolved; current landing page has no logo.
- **Logo resolved**: wordmark "Cubic" in Darker Grotesque (loaded via `next/font/google` in `layout.tsx`, applied on `<body>`), weight 800, white on black. The period is an **outlined square dot** (echoes the network-node squares), not a filled mustard dot as in the user's reference — monochrome flip. Slogan on page: "Cloudflare for agent actions." + subline "Every agent tool call checked against identity, intent, policy, and trust." Page metadata updated to match.
- Landing page completed with header + footer. New `app/src/components/Logo.tsx` (reusable wordmark, square-dot uses `currentColor`). globals.css gained `.mono`, `.link`, `.btn-outline` helpers. Header: logo / nav (PRODUCT, NETWORK, DOCS — placeholder `#` links) / GET ACCESS outlined button. Footer: © 2026 CUBIC / GITHUB·DOCS·CONTACT / "AGENT AUTHORIZATION GATEWAY". User wants to review and correct styling choices.
- **UI convention locked in** (recorded in DESIGN.md): monochrome only; floating panels/dialogs use macOS-style window chrome (rectangle + 34px title bar + three monochrome circles ✕ − ⤢) via `app/src/components/MacWindow.tsx` — reuse it, don't recreate chrome.
- Landing page is now scrollable: header made sticky, hero pinned to first viewport (`calc(100dvh - 64px)`), added manifesto statement section below (large Darker Grotesque 700, ~46px, two-tone — final clause dimmed gray), footer at page end.
- Header restyled to a floating pill bar (per user reference): sticky, `width: min(1040px, 86vw)`, margin 14px auto, bg #0d0d0d, 1px #232323 border, 12px radius, height 54 — logo left, nav + GET ACCESS right. Hero height now `calc(100dvh - 82px)`. Header logo later bumped to 24px.
- Added team section (MacWindow with two dithered portraits left, quote-style copy right). New `app/src/components/DitherImage.tsx`: grayscale+contrast filter with dot-matrix overlay (newsprint/halftone effect). User to drop photos at `app/public/founder-1.jpg` + `founder-2.jpg` (placeholder frames show until then); names/roles still placeholders.
- MacWindow redesigned per user reference: rounded 10px rect, 44px bar, three PLAIN circles right (○ ○ ●, no glyphs), optional browser-style tabs left via `tabs`/`activeTab`/`onTabChange`. Team section extracted to `app/src/components/TeamSection.tsx` (client) — single image area with tabs `tron.jpeg` (live, `/dev_imgs/tron.jpeg`) and `duo.jpeg` (waiting on `/dev_imgs/duo.jpeg`). DESIGN.md convention updated to match.
- **MacWindow chrome hard-won lessons** (cost several iterations): (1) tab/separator geometry is ONE measured SVG layer spanning the window width — HTML borders + overlays can't produce continuous geometry; (2) measure with `offsetWidth`/`offsetLeft`, NOT `getBoundingClientRect` — the section's entrance-animation transform scaled measurements by 0.78, misaligning the outline; (3) after the entrance animation, set `transform: "none"` — a leftover `scale(1)` layer caused bilinear sampling that dimmed 1px SVG strokes ~50%. Debug method that worked: playwright-core + headless Brave screenshots/DOM dumps (`/tmp/pw/`), section has `id="team"`.
- **MacWindow tab outline simplified** (user flagged as bug): the old flare/notch dipped 9px BELOW the title-bar separator and, with a black mask rect over the content, read as a dangling outlined "tag" hanging over the image. Removed NOTCH/TIP_Y/OUT, the flare path, and the mask rect — tab sides now run down to the separator with CONCAVE bottom corners (radius = CORNER, 9px) that flare outward and merge into the separator line (per user's "Prize" reference), which is broken under the active tab. Separator fill rects extend to L/R ∓ (CORNER-1) to stay continuous with the curve ends. SVG overlay is exactly BAR (44px) tall, no content overlap. Verified via playwright screenshots incl. tab switching.
- **MacWindow: uniform tab widths** — all tabs share the widest tab's natural width (`tabW` state; buttons get `minWidth: tabW`, content centered; `tabW` in the layout-effect deps so the outline re-measures after enforcement). This makes the second tab as wide as `tron.jpeg` instead of hugging its shorter label.
- **TeamSection: auto-rotate + crossfade** (user request): profile auto-switches every 7s (`ROTATE_MS`, interval starts once the section is visible via the existing IntersectionObserver; `activeTab` in the effect deps so a manual click resets the 7s clock). Both portraits now render stacked (absolute inset-0) inside a shared `aspectRatio: 1` container and crossfade via `opacity 0.6s ease` — no snap. Tab set is now `tron` + `min` (user added `app/public/dev_imgs/min.jpeg`; the old `duo.jpeg` naming is dead). Note: min's photo is very bright (white shirt) so the halftone dither reads washed-out vs tron's — flag to user if they dislike it.
- **DitherImage gained a `brightness` prop** (default 1.05) — per-image tone knob; min uses 0.85, tron stays default.
- **Manifesto: scroll-driven word reveal** (user request, replaced the static bright+dim two-tone text): new client component `app/src/components/Manifesto.tsx` splits the statement into word spans; a scroll listener (rAF-throttled, passive) computes progress `p` of the paragraph through the viewport (`(vh*0.85 - top) / (vh*0.45 + height)`, clamped 0..1) and interpolates each word's color `#3a3a3a → #f4f4f4` with an 8-word soft ramp (`t = clamp((p*(n+RAMP) - i)/RAMP)`). All words start grey, brighten top-to-bottom as the user scrolls. Note for verification: playwright element screenshots force the element fully into view, which clamps the scroll position — evaluate color values directly if precise mid-states must be checked.
- **Team quote: back to plain static text** (user reversed course after trying decode-scramble — "we're doing too much"): the quote is a fixed `<p>` in `#e8e8e8`, single tone (no dim clause), no cursor, no text animation. LESSON: user prefers restraint — confirm appetite for flourish before adding text effects; the manifesto scroll-reveal stayed because it aids reading.
- **Hero copy rebuilt** (user found the old hero underwhelming): new client component `app/src/components/HeroCopy.tsx` renders the whole left column — bigger wordmark (`clamp(88px,11vw,160px)`) and slogan, the old subline DELETED, replaced by "Your {model} can't escape the cube □" with the model name deleting letter-by-letter then typing the next one (`hold` 1100ms → `delete` 40ms/char → `type` 65ms/char, state machine in HeroCopy; user rejected both the instant swap AND a blinking cursor on the model name — keep the model span bare) through `MODELS` (Fable 5, GPT Astra, Grok 4.6, Gemini 3 Ultra, Llama 5, DeepSeek V4, Mistral 4, Qwen 3 Max — user-supplied fictional 2026 names, extend freely); the pipeline line `intent → policy → capability → execution` now TYPES out char-by-char (34ms/char, 600ms delay) with the blinking block cursor; below that a mock stats grid of FOUR stats on one line (user removed DENIED — "people can subtract that themselves": 1,842 agents online / 24,921 intents evaluated / 23,884 authorized / 123 escalated) with a `SIMULATED ACTIVITY — DEMO NETWORK` tag (PROJECT.md requires mock counters be clearly marked until real telemetry exists). Left column widened to 46%/min 440px.

### Important Constraints
- Authorization decisions must be deterministic; LLMs may classify/summarize but must not be the final ALLOW/DENY oracle.
- The gateway should authorize normalized tool operations and must not become an API-adapter zoo.
- Detailed private tenant audit data stays off-chain; blockchain is used selectively for trust, identity, reputation, attestations, payments, and verifiability.
- Ledger hardware security must not be represented as equivalent to a mock/dev implementation.

### Decisions
Record architectural decisions here as they are made.

### Known Issues / Open Questions
Record unresolved implementation questions here.

### Recent Changes
- **Partner marquee + product details sections** (user request): `app/src/components/PartnerMarquee.tsx` — infinite CSS marquee (36s loop, `marquee` keyframe + `.marquee-track` in globals.css, pauses on hover) of placeholder partner names (THE GRAPH · HEDERA · LEDGER · ERC-8004 · AGENT0 · BLOCKY402 · MCP) separated by outlined-square markers; user will swap in logos later. `app/src/components/DetailsSection.tsx` — "THE PRODUCT / Every call, interrogated." section with 3 MacWindow cards (`title` prop, no tabs): 01/THE GATE (intent normalization, deterministic allow/deny/escalate, capabilities not keys), 02/TRUST CONTEXT (ERC-8004/Agent0 via The Graph, Ledger hardware approval for high-risk, full audit chain), 03/MACHINE PAYMENTS (x402 invoices as intents, spend policy, Hedera settlement via Blocky402). Copy drafted from PROJECT.md for user review — expect edits. Page order now: hero → manifesto → team → marquee → details → footer. DetailsSection is a client component with a staggered pop-IN entrance per card (IntersectionObserver on the cards GRID with `rootMargin "0px 0px -25% 0px"` + threshold 0.1 — earlier section-level/-20% trigger fired too early per user; each card `scale(0.55)` → `none` centered, back-ease `cubic-bezier(0.3,1.35,0.55,1)` for the mac-window open feel, 140ms stagger, ends at `transform: "none"` per the MacWindow measurement lesson). Cards equalized via grid stretch + wrapper `display:"flex"` + `MacWindow style={{ flex: 1 }}`. Card type bumped per user: heading 21→23, body 14.5→15.5, bullets 13.5→14.5.
- **Responsive pass** (user request): layout-critical inline styles extracted to `globals.css` classes with media queries at **1024px** (tablet) and **640px** (phone) — `.site-header` (nav hidden ≤640px), `.hero` / `.hero-copy` / `.hero-canvas-wrap` (split → stacked, canvas absolute behind copy with `to top` mask ≤1024px), `.section-pad`, `.team-grid` (2-col → 1-col, image maxWidth 460 centered), `.details-grid` (3-col → 1-col), `.site-footer` (wraps, `.footer-tag` hidden ≤640px). MacWindow untouched (ResizeObserver handles remeasure). Minor: Manifesto maxWidth `min(1080px,100%)`, marquee separator/font clamps, hero stats minmax 104→96. No overflow at 1440/834/390; desktop ≥1024px rendering byte-identical. No hamburger (DESIGN.md restraint) — nav links hide, GET ACCESS stays.
- **NetworkCanvas mobile** (user request): canvas is completely removed ≤1024px — `.hero-canvas-wrap` is `display:none` in globals.css media query (NetworkCanvas component itself reverted to original always-animating form; nothing renders so no CPU cost). Desktop >1024px unchanged. Verified: phone `display:none`, desktop `block`. Follow-up: the ≤1024px media query must also zero the desktop `height: calc(100dvh - 82px); min-height: 540px` on `.hero` (set `height:auto; min-height:0`) or the empty space reappears — hero is now content-height on mobile.
- **Mock /network page** (user request): new route `app/src/app/network/page.tsx` (client) — eyebrow + "Available agents." + mock stats + `SIMULATED ACTIVITY — DEMO NETWORK · SOURCE: MOCK — ERC-8004 WIRING PENDING` tag; `.network-grid` (7fr/5fr → 1-col ≤1024px): left MacWindow `LIVE TOPOLOGY` with new `app/src/components/AgentGraph.tsx` (canvas in the NetworkCanvas visual language — outlined squares sized by reputation, ring layout + cross-links, traveling pulses, click/hover select, `#registryId` labels, monochrome status dimming), right MacWindow detail (square glyph, name/agentKey/status, reputation bar, AUTHORIZED/ESCALATED/DENIED, REGISTRY ID/ENS/OWNER/CHAIN/LAST SEEN); bottom DIRECTORY `.agent-grid` (4→2→1 col) of buttons syncing selection with the graph. Data in `app/src/data/agents.ts` (`Agent` type mirroring PROJECT.md §4 + `getAgents()` async stub for the future ERC-8004/subgraph swap; 12 mock agents). Landing header nav now links NETWORK → `/network` via `next/link` (PRODUCT → `/`). `pnpm typecheck` + `pnpm build` pass (`/` + `/network` prerendered).
- **Console dashboard + wallet gate** (user request): header nav is now CONSOLE / NETWORK / DOCS via shared client `app/src/components/SiteHeader.tsx` (active-link highlight); GET ACCESS replaced by CONNECT WALLET mock (`app/src/components/WalletProvider.tsx` context, address `0x3f…9a1c`, provided in root `layout.tsx`; button shows address + disconnect when connected). `/console` layout (`app/src/app/console/layout.tsx`): sticky sidebar nav (OVERVIEW/AGENTS/TASKS/POLICIES/APPROVALS×3/AUDIT/PAYMENTS/SETTINGS, wraps to top bar ≤1024px) + wallet gate — disconnected users see a centered `CONSOLE — LOCKED` MacWindow with connect CTA, no content. 8 mock subpages from `app/src/data/console.ts` (PROJECT.md §4/§12 shapes): overview (stats + pending approvals + recent decisions + spend bar), agents (registry table + declared caps), tasks (intent allow/esc/deny breakdown), policies (client toggles), approvals (client approve/deny queue with Ledger-hardware flags + session-resolved list), audit (intent→decision→capability→result table), payments (x402 invoices), settings (tenant config). Shared bits in `app/src/components/ConsoleBits.tsx` (PageHead/StatRow/Panel/DecisionTag/table styles). `pnpm typecheck` + `pnpm build` pass, 13 routes prerendered.
- **Market section v3 — bare + click-to-lock** (user fix): MacWindows removed from both columns — squares and ledger stand bare (user: the squares are genius as-is, windows cheapen them). Hover flaw fixed: selection is now click/tap-to-lock, hover only previews brightness, so the readout's source link never swaps while the cursor travels toward it; legend tabs are click-only too. Animations kept (draw-in, count-up, readout crossfade, staggered rows). Build + typecheck pass.
- **Decision feed + closer + footer** (user request, 1/2/5): `DecisionFeed.tsx` (client) between details and market — `GATEWAY — LIVE DECISIONS` MacWindow streaming 7 mock intent→decision→capability lines (ALLOW filled tag, ESCALATE/DENY outline, blinking cursor, SIMULATED tag, 2.6s tick, loops); `Closer.tsx` (client) after market — square mark, "Put your agents inside the cube.", CONNECT WALLET wired to real wallet state (ENTER CONSOLE when connected) + READ DOCS; `SiteFooter.tsx` shared by `/`, `/network`, `/console` — brand + status lines, PRODUCT/PROTOCOL/COMPANY columns (incl. FAQ placeholder link), bottom bar with © + CUBIC.ETH teaser. Page order: hero → manifesto → team → marquee → details → feed → market → closer → footer. Build + typecheck pass, 13 routes.
- **FAQ page** (user request): `/faq` — eyebrow + "Asked, answered." + `FaqList.tsx` client accordion (10 Qs drafted from PROJECT.md: what/authorization flow/API keys/LLM-not-oracle/escalations+Ledger/payments via x402+Hedera/Graph role/vs OPA-Vault-MCP/onchain vs offchain/access). Rows are hairline-separated with square +/– toggles, single-open, answers crossfade via `ring-in`. Header nav now CONSOLE/NETWORK/FAQ/DOCS (active highlight on `/faq`); footer FAQ placeholder points to `/faq`. Build + typecheck pass, 14 routes.
- **Closer canvas** (user request): `CloserCanvas.tsx` behind the CTA — "inside the cube" tunnel of outlined squares drifting outward from center, growing + fading as they approach (near ones get a filled core), slow breathing scale, radial mask to black at edges so copy stays legible. Pauses offscreen (IO), static frame for prefers-reduced-motion, DPR ≤ 2. Build + typecheck pass.
- **Network agent labels always-on** (user: cubes should show names): `NetworkGraph.tsx` now draws every agent's 8-char pseudonym under its cube at all times (dim white; bright on hover/select) instead of only on hover/select. Real agent keys stay off this surface by design — pseudonym is the name here. Verified via screenshot. Typecheck + build green.
- **Demo chat free-text now BLOCKs through the gateway (user request 2026-09-13)**: previously the parser self-censored — system prompt forced `{tool:null}` for deletes/secrets/spends and `validateParsed` rejected anything outside the 5 MCP shapes, so adversarial free text died as display-only no-tool and never photographed as a Decision. Now the LLM drafts the closest `scope.action` call (even disallowed: delete_repo, .env read, deploy, treasury.*) and `validateParsed` passes tool-like names through verbatim; the deterministic policy engine DENYs for real (`tool_not_allowed`, `secret_resource`, …). Security boundary unchanged (LLM drafts, policy decides; task_id/reasoning_ref still stripped; unknown tools still deterministically deny). UI stays monochrome per DESIGN.md: deny banner reads `Decision: Deny — BLOCKED · <reason>` + `GATEWAY INTERCEPTED — LLM DRAFTED, POLICY DENIED` caption, tool-turn replies labeled `LLM DRAFT`, input hints `the LLM drafts, the gateway decides`. `demo-chat.test.ts` parse contract updated; 16/16 green, typecheck clean. Note: full-suite run shows 1 unrelated pre-existing fail (`treasury.test.ts` live LangSmith read-back, env-dependent).
- **Demo chat agentgate-parity (user request 2026-09-13 — two voices every turn)**: reference is `~/coding/agentgate-app` (`chat/route.ts` refusal-vs-proposal split, `llm.ts` tool-call with prose-refusal fallback, `AgentChatPage.jsx` refusal guardrail card + risk/rules detail panel). Fixed the screenshot inconsistency (one off-scope message showed the model's redirect, the next showed the bare `NO_TOOL_MESSAGE` — both were no-tool; the difference was whether the model's safety refusal contained a JSON block): prose-without-JSON now surfaces trimmed to 280 chars as the reply, and provider-failure/empty paths fall back to a canned `NO_TOOL_REDIRECT` in `chat.ts`, so a no-tool turn always speaks. Deny turns now show a `POLICY ENFORCEMENT DETAIL` block (policy, violated rule, all reason codes, risk score — data the turn already carried, previously only `reasons[0]` was visible) plus an `AGENT — OUT OF SCOPE` label on no-tool turns. Demo-surface only, zero engine changes. 18/18 demo-chat green, typecheck clean.
- **PR-12 parse kill + agent picker (user fury 2026-09-13)**: (1) "Read PR number 12" died as OUT OF SCOPE even though reads are allowed — the gateway never saw it; the strict parser rejected the draft (model omits `repo` for bare "PR 12", pr can arrive as a string) and the new canned redirect made the agent look self-contradictory. Parser now forgives: `repo` defaults to `acme/backend`, `target` to `acme/backend#421`, `pr` coerces to int; garbage still fails. (2) Chat had no visible identity — everything ran as hardcoded `agent:8472`. Now an AGENT dropdown (demo-tenant agents in seed order: deploy-agent 8472, lab-1, treasury, reader) with a `SPEAKING AS` header; scenario buttons filter to shared (`agent:8472`-labeled) + that agent's pinned templates; POST accepts `agent_key` (template pins still win; unknown → 400 `unknown agent:`); switching speakers resets the session and the server mints a fresh Chat task rather than merging into another agent's session. SOUL.md/MEMORY.md/granted-tools per agent already exist via the console agent-profile route (`readDoc(agentKey, …)` + declaredCapabilities) — chat picker covers identity + templates; profile docs stay in console. 23/23 demo-chat green, typecheck clean.
- **Amends: agent speaks AS itself (user fury 2026-09-13)**: treasury-agent answered "Hi, who are you" in deploy-speak because the parser prompt carried zero identity. New `server/demo/identity.ts` loads the speaker's SOUL.md + MEMORY.md (same slug rule as the console profile route, 1500-char caps, nulls when absent) + granted tools; `parse.ts` builds the system prompt per agent (`buildSystemPrompt` — identity block first, mapping contract unchanged) and `runChatTurn` resolves the speaker BEFORE parsing, passing identity into `parseFreeText`. No-tool fallback is now per-agent (`redirectFor`: name + granted tools) — the old generic `NO_TOOL_REDIRECT` is deleted. Tests: soul/memory load, prompt contains voice+tools, provider receives identity prompt, per-agent redirect for 8472 vs treasury. 28/28 demo-chat green, tsc clean. Incidental: afterAll now deletes `councils` rows before tenants (worktree drift — another uncommitted change seeds per-tenant councils with an FK; teardown tripped without it).
- **Real reads vs simulated writes (user decision 2026-09-13)**: `GithubExecutor` already has real mode (GITHUB_TOKEN → live API, `mode:"real"` in the execution line) but the checked-in token is dead (401 Bad credentials) — everything has been silently mock. Call: reads go REAL (fresh token + a real repo/PR target, zero code changes; free text accepts explicit repos), merge stays real-capable but approval-gated and off-camera by default, deploy stays mock-always and treasury dev-mode by decision (production/money mutations on camera = no) — but their mock bodies are now camera-worthy: deploy returns a deterministic Vercel-flavored ticket (deployment id, URL, inspector link, READY, labeled mock), treasury returns a DEX-flavored quote (pair from resource, venue, tx hash, labeled dev). Mock github read bodies untouched (pinned by execution/mcp tests). 49/49 across execution+treasury+demo-chat green. To light up real reads: fresh `GITHUB_TOKEN` (repo-read scope) + tell me which repo/PR to aim at.
- **Full-fiction posture + Acme tenant profile (user decision 2026-09-13)**: supersedes the real-reads idea above — user killed it: no real repos, `acme/backend` stays fictional, demo mocks everything and says so. New `docs/acme-tenant.md`: Acme the fictional 120-person fintech SaaS (invoicing product, GitHub monolith + onchain USDC/ETH treasury), the 4-agent AI workforce cast, 6 pilot incidents each mapped to a demo beat id, why-Cubic section mapped to product capabilities, and a fiction boundary (repos/PRs/secrets/deploys/quotes simulated + labeled; identities, decision records, authorization math real). Next: draft the demo script/story/transcript off this doc.
- **VERIFY ON HCS rendered page (user fury 2026-09-13)**: the approvals link pointed at the raw JSON API (`target=_blank` to `/api/anchors/verify`), so clicks landed on an undifferentiated JSON blob. Route now content-negotiates: `Accept: text/html` → rendered monochrome verify page (per-event VERIFIED ✓ / PENDING / NOT-ANCHORED verdicts, fingerprints, OPEN TOPIC ON HASHSCAN link to the real topic explorer — verified live: `hashscan.io/testnet/topic/0.0.10517868` loads with messages); JSON envelope byte-identical for API/tests. Approvals link stays in-app (no more blank-tab JSON). tsc clean, councils 5/5 green (route contract untouched). NOTE: worktree carries unrelated uncommitted drift (approvers page, derive, config, seed) — my diff is verify route + approvals link only.
- **Agent follow-up synthesis (user order 2026-09-13)**: chat was one-shot — tool result sat as a summary line, agent never answered. Now `synthesizeFollowUp` in parse.ts feeds the result back to the model (same voice/identity, plain prose, result-is-truth) on allow+succeeded only; renders as AGENT FOLLOW-UP below the execution line. Deny/escalate/failures/payment-holds/lifecycle: no synthesis, enforcement display stands alone. Follow-up never re-enters gateway (no auth loop); provider unset/fail → null, turn renders as before. Also stripped the `(mock)` parenthetical from the PR summary (full-fiction posture — tenant doc owns disclosure; `mode:"mock"` field stays the honest signal) + fixed 6 pinned test expectations. Rollback: `d893254`. 57/57 green (demo-chat+execution+mcp+treasury), tsc clean, committed `4e7e13f`.
