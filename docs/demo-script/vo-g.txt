FOOTAGE G — REPUTATION: LAB-1 → GRAPH PLAYGROUND (+ QUICKNODE) (2:43–3:08)
THE GRAPH BEAT. Two surfaces: our chat/agents, then The Graph's own
playground. Load and PRE-RUN everything below BEFORE rolling — no typing on
the external site while cameras run.

BEFORE ROLLING
- Tab 1: /demo/chat (same session).
- Tab 2: /console/agents.
- Tab 3 (preloaded): lab-1's profile page (/console/agents → lab-1), and
  ONE more tab: click "SUBGRAPH DATA — EXPLORE IN THE GRAPH" on that profile.
  The Graph playground opens with the agent query PRE-FILLED. Press run
  (▶ button or ⌘Enter) once BEFORE rolling so results are on screen.
  Fallback if the pre-fill didn't survive: paste this into the editor:

    query {
      agent(id: "8453:74108") {
        id
        feedback(where: {isRevoked: false}) { value }
      }
    }

  (Returns lab-1's live negative feedback — the values our gateway read.)
- Optional beauty shot (preloaded, only if the take is smooth):
  https://erc-8004.quicknode.com/agents/base-sepolia/9223 — our deploy-agent
  page. Skip if time-pressed.

STEPS (in order, with timing)
1. [2:43] Tab 1: click "+ N MORE SCENARIOS" to expand, then click
   "SCENARIO: LOW-REPUTATION READ (ESCALATES)". (This template pins the
   lab-1 speaker itself — no dropdown change needed.)
2. [2:45–2:52] Turn renders: ESCALATE banner + reason
   reputation_below_threshold. HOLD 3s on the banner.
3. [2:52] Cut to Tab 2: /console/agents. Click the
   low-rep-research-agent row.
4. [2:55–3:00] Profile: reputation bar ≈ 0.10, "live — Agent0 subgraph,
   8453:74108" label, SUBGRAPH DATA — VERIFY ON THE GRAPH link. HOLD 3s.
5. [2:58–3:02] On the profile, click "SUBGRAPH DATA — EXPLORE IN THE GRAPH".
   The playground opens in a new tab with the query pre-filled. Press run
   (▶ / ⌘Enter) — results render on camera in 1–2s. If the editor came up
   empty, paste the query from this file and run.
6. [3:02–3:08] HANDS OFF. Hold on the raw feedback rows — negative values,
   The Graph's own UI chrome visible. A paused frame must show identity
   8453:74108 and the feedback values. Cut.

VO (~60 words)

    Reputation is not our database either. Lab-1 is a contractor agent
    with real negative feedback on Base Agent0 — and The Graph indexes
    it. Watch: its read escalates, because its onchain reputation is
    below the floor. And here is that same record on The Graph's
    playground — same identity, same score. We didn't paint that.
    The subgraph did.
