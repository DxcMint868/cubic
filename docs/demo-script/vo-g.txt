FOOTAGE G — REPUTATION: LAB-1 → GRAPH PLAYGROUND + QUICKNODE (2:51–3:16)
THE GRAPH BEAT. Two live external surfaces, preloaded BEFORE rolling:
(a) The Graph playground with the agent query for 8453:74108 already run,
raw feedback rows visible; (b) QuickNode agent page
erc-8004.quicknode.com/agents/base-sepolia/9223 (our deploy-agent) as the
beauty shot if time allows. Console: /console/agents → lab-1 profile first,
then cut to the playground.

SHOT LIST
1. 2:48–2:56 — chat: "As lab-1: read PR #421" → ESCALATE
   reputation_below_threshold. Hold on the banner.
2. 2:56–3:04 — /console/agents → lab-1 profile: reputation bar ~0.10,
   SUBGRAPH DATA link visible.
3. 3:04–3:13 — Graph playground: the same identity, the same negative
   feedback — raw, on The Graph's infra. STILL 4s. (Optional 2s of the
   QuickNode page if the take is smooth.)

VO (~60 words)

    Reputation is not our database either. Lab-1 is a contractor agent
    with real negative feedback on Base Agent0 — and The Graph indexes
    it. Watch: its read escalates, because its onchain reputation is
    below the floor. And here is that same record on The Graph's
    playground — same identity, same score. We didn't paint that.
    The subgraph did.
