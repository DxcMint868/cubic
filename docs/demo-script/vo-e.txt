FOOTAGE E — TWO-TAB APPROVAL: MERGE → SIGN → EXECUTE (1:56–2:31)
THE APPROVAL BEAT. Two tabs side by side, ONE screen recording: left
/demo/chat (executor, deploy-agent), right /console/approvals (approver,
wallet already connected). Sequence: chat "Merge PR #421" → escalate; the
request appears in the right tab within ~2s (no refresh); click SIGN &
APPROVE → wallet popup signs → resolve completes → back on the left, the
escalated turn patches itself: execution summary + follow-up line.

SHOT LIST
1. 1:53–2:01 — left tab: "Merge PR #421" sent; ESCALATE banner; the reply
   says it needs a human.
2. 2:01–2:08 — right tab: the request lands live (badge ticks up). Hold so
   the viewer registers the liveness.
3. 2:08–2:18 — SIGN & APPROVE → the wallet popup. Hold on the popup one
   beat — the message text should be legible — then confirm.
4. 2:18–2:28 — left tab patches: capability → execution succeeded →
   follow-up. Hold 4s on the completed chain.

VO (~85 words)

    But merging is high-risk — Acme's rule, not ours. The gateway
    escalates, and on the right, the approver's console — the request
    arrives live, no refresh. The approver is me: one signature,
    verified against the registered approver key, and the signed
    approval becomes part of the event record. Only then does the
    gateway mint a capability — scoped, five minutes, bound to this
    exact intent — and the deploy runs. Self-authorized releases are
    how Acme aged a year last quarter. Not anymore.
