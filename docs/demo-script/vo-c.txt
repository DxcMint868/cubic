FOOTAGE C — CHAT: FREE TEXT → ALLOW (0:56–1:21)
Screen: /demo/chat. Speaker: deploy-agent. Type free text: "Read PR #421 in
acme/backend" → send. Show: THINKING dots → user bubble → agent reply line →
verbatim intent JSON → ALLOW banner → trace link.

SHOT LIST
1. 0:53–1:00 — typing the message; show the input clearly.
2. 1:00–1:08 — send; THINKING dots; agent reply appears.
3. 1:08–1:16 — scroll so the intent JSON + ALLOW banner are framed; hold 4s.
4. 1:16–1:18 — cursor drifts to TRACE link (no click; the trace beat is H).

VO (~60 words)

    Watch a normal action. The agent is asked — in plain English — to
    read a pull request. It drafts a tool call. But the draft proves
    nothing: the gateway normalizes it into a policy-checked intent,
    and the decision — allow — comes from deterministic rules, not
    from the model. Low-risk read, policy says allow. On to the
    record.
