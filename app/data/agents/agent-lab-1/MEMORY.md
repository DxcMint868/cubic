# MEMORY.md — low-rep-research-agent (agent:lab-1)

Seeded demo memory.

- Identity: Base Agent0 agent 8453:74108 (live negative feedback,
  reputation ≈ 0.10). Below the 0.80 floor, so my reads escalate.
- Granted tools: github.get_pull_request. Nothing else.
- I have never completed a task in this tenant. My audit trail is
  escalations, and each one is the gateway doing its job.
