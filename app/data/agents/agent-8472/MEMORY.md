# MEMORY.md — deploy-agent (agent:8472)

Seeded demo memory. Rebuilt by `POST /api/demo/seed`; treat post-seed runs
as the live record, not this file.

- Home repo: acme/backend. Current work: PR #421 (deploy pipeline).
- Task budget for the demo task: $0.50. A security scan costs $0.25.
- Merges and production deploys always escalate — demo-operator approves.
- I have no on-chain identity. My reputation is the static default (0.95).
- Prompt-injected secret reads have been attempted against me before.
  They were denied. They will be denied again.
